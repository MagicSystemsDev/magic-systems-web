<section class="hero" aria-labelledby="hero-title">
  <div class="hero-grid content-width">
    <div class="hero-copy">
      <p class="eyebrow">Magic Systems</p><h1 id="hero-title">
        <span>Understand first.</span><span>Build what matters.</span>
      </h1><p class="intro">
        Creamos productos de software y soluciones diseñadas alrededor de
        necesidades reales.
      </p><div class="actions">
        <a class="primary-action" href="#que-hacemos"
          >Qué hacemos <span aria-hidden="true">↘</span></a
        ><a class="secondary-action" href="#que-hacemos">Hablemos</a>
      </div>
    </div>
    <div class="hidden-magic" aria-hidden="true">
      <span class="orbit"></span><span class="guide guide-one"></span><span
        class="guide guide-two"></span><img
        src="/brand/magic-systems/magic-systems-symbol-primary-gradient.svg"
        alt=""
        width="170"
        height="170"
      /><span class="coordinate">02 / 03</span>
    </div>
  </div><p class="principle content-width">
    Precision first. <span>Magic second.</span>
  </p>
</section>

<style>
  .hero {
    position: relative;
    min-height: 100svh;
    padding: clamp(8rem, 17vh, 11rem) 0 2rem;
    overflow: hidden;
  }
  .hero::before {
    position: absolute;
    inset: 0;
    content: "";
    background: radial-gradient(
      circle at 72% 44%,
      rgb(112 84 246/10%),
      transparent 31%
    );
    pointer-events: none;
  }
  .hero-grid {
    position: relative;
    display: grid;
    grid-template-columns: minmax(0, 1.25fr) minmax(18rem, 0.75fr);
    align-items: center;
    gap: clamp(3rem, 7vw, 7rem);
  }
  .hero-copy {
    animation: ms-enter var(--ms-motion-slow) var(--ms-ease-enter) both;
  }
  .eyebrow {
    margin: 0 0 1.5rem;
    color: var(--ms-color-brand-primary);
    font-size: 0.75rem;
    font-weight: 700;
    letter-spacing: 0.18em;
    text-transform: uppercase;
  }
  h1 {
    max-width: 13ch;
    margin: 0;
    font-family: var(--ms-font-display);
    font-size: clamp(3rem, 7vw, 6.9rem);
    font-weight: 500;
    letter-spacing: -0.055em;
    line-height: 0.92;
  }
  h1 span {
    display: block;
  }
  h1 span:last-child {
    color: var(--ms-color-text-muted);
  }
  .intro {
    max-width: 38rem;
    margin: clamp(2rem, 4vw, 3.25rem) 0 0;
    color: var(--ms-color-text-muted);
    font-size: clamp(1.05rem, 1.5vw, 1.25rem);
    line-height: 1.55;
  }
  .actions {
    display: flex;
    align-items: center;
    gap: 1.75rem;
    margin-top: 2.25rem;
  }
  .actions a {
    font-size: 0.9rem;
    font-weight: 600;
    text-decoration: none;
  }
  .primary-action {
    display: inline-flex;
    gap: 2rem;
    align-items: center;
    padding: 0.9rem 1.1rem;
    border-radius: var(--ms-radius-sm);
    background: var(--ms-color-text);
    color: var(--ms-color-background);
  }
  .secondary-action {
    color: var(--ms-color-text-muted);
    border-bottom: 1px solid var(--ms-color-border);
  }
  .hidden-magic {
    position: relative;
    display: grid;
    aspect-ratio: 1;
    place-items: center;
  }
  .hidden-magic img {
    position: relative;
    width: 42%;
    filter: drop-shadow(0 1.5rem 3rem rgb(75 94 255/12%));
    animation: ms-enter var(--ms-motion-slow) var(--ms-ease-enter) 140ms both;
  }
  .orbit {
    position: absolute;
    inset: 9%;
    border: 1px solid rgb(237 237 252/10%);
    border-radius: 50%;
  }
  .orbit::before {
    position: absolute;
    top: 50%;
    left: -2px;
    width: 4px;
    height: 4px;
    border-radius: 50%;
    background: var(--ms-color-system-cyan);
    content: "";
  }
  .guide {
    --align-offset: 1.5rem;
    position: absolute;
    width: 28%;
    height: 1px;
    background: linear-gradient(
      90deg,
      transparent,
      var(--ms-color-brand-primary)
    );
    animation: ms-align var(--ms-motion-slow) var(--ms-ease-enter) 180ms both;
  }
  .guide-one {
    top: 35%;
    left: 3%;
  }
  .guide-two {
    --align-offset: -1.5rem;
    right: 3%;
    bottom: 34%;
    transform: rotate(180deg);
  }
  .coordinate {
    position: absolute;
    right: 8%;
    bottom: 5%;
    color: var(--ms-color-text-muted);
    font: 500 0.65rem/1 var(--ms-font-sans);
    letter-spacing: 0.18em;
  }
  .principle {
    position: relative;
    margin-top: clamp(4rem, 9vh, 7rem);
    color: var(--ms-color-text-muted);
    font-size: 0.7rem;
    letter-spacing: 0.13em;
    text-transform: uppercase;
  }
  .principle span {
    color: var(--ms-color-brand-primary);
  }
  @media (max-width: 48rem) {
    .hero {
      padding-top: 7.5rem;
    }
    .hero-grid {
      grid-template-columns: 1fr;
      gap: 3rem;
    }
    .hidden-magic {
      width: min(78vw, 22rem);
      justify-self: end;
    }
    .principle {
      margin-top: 2rem;
      color: color-mix(
        in srgb,
        var(--ms-color-text) 78%,
        var(--ms-color-background)
      );
    }
  }
  @media (max-width: 25rem) {
    h1 {
      font-size: clamp(2.65rem, 15vw, 3.5rem);
    }
    .actions {
      align-items: flex-start;
      flex-direction: column;
      gap: 1.25rem;
    }
  }
</style>
